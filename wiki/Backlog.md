# KeyoneKb2 Feature Backlog

* Some applications attach a button (e.g., "Continue") to the keyboard (if the swipe panel is hidden, the buttons are not visible either)

* New plugin type: "tap on OK"

* Setting: Do not open NAV-pad (create a setting or find a workaround)

* Setting: Disable entering gesture mode by double-tap

* Feature: Bind screen coordinate click to the keyboard

* Feature: Invoke a Tasker script

* Bottom sensor delay causes infinite scrolling

* ? KEY_0 + gestures scrolls or moves pointer

* ?! Implement dynamic cursor speed like in bb.kbd

* Hints/tooltips

* Custom text selection mechanism with Shift (notes)

* Swipe left deletes a word

* Zoom via keyboard swipes

* Service settings accessible from key remapping
